create table BOXER (
	BOXER_ID int primary key,
    BOXER_FIRST_NAME varchar(100),
    BOXER_LAST_NAME varchar(100),
    WEIGHTCLASS varchar(20),
    RATING int,
    HEIGHT varchar(4),
    WEIGHT varchar(6),
    FNC varchar(1),
    FNF varchar(1),
    ACTIVE varchar(1)
);
